meta {
  name: Cambiar estado de versión (admin)
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/app-version/APP_VERSION_ID_AQUI/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
