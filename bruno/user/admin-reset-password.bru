meta {
  name: Admin resetear contraseña de usuario
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/api/user/USER_ID_AQUI/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "new_password": "nueva1234"
  }
}
